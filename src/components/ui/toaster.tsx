import React from "react";

/**
 * Disabled Toaster component to prevent React hooks issues during initialization
 * This returns null to avoid any rendering issues
 */
export function Toaster() {
  return null;
}